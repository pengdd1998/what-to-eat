# 「吃什么」边缘（chishenma.top＝P-c 正式主机名，2026-09-25 迁移；
# 旧 whattoeat.lifestyle 未获工信部批复不可备案，已弃——ADR-005 修订节）
# chishenma.top 已完成 ICP 备案（备案号见页脚），微信内可达性随备案解除拦截
chishenma.top {
	encode gzip
	# §4.1 评审 8 第三层：公网 /api/admin 一律 403（admin 真身仅宿主回环 8001）
	handle /api/admin/* {
		respond 403
	}
	reverse_proxy api:8000
}
